// I AM NOT DONE

const ime: number = 'James';
const godine: string = 25;

// DO NOT CHANGE THE CODE BELOW
function pozdrav(personName: string, personAge: number): string {
  return `${personName} ima ${personAge} godina.`;
}

console.log(pozdrav(name, age));

// not important
export {};
