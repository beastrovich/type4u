//- I AM NOT DONE

const name: number = 'James';
const age: string = 25;

// DO NOT CHANGE THE CODE BELOW
function createGreeting(personName: string, personAge: number): string {
  return `${personName} is ${personAge} years old.`;
}

console.log(createGreeting(name, age));

// not important
export {};
