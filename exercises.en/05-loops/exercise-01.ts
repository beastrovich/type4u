// I AM NOT DONE

function doubleNumbers(numbers: Array<string>): void {
  for (let i = 0; i < numbers.length; i++) {
    const result = numbers[i] * 2;
    console.log(result);
  }
}

// DO NOT CHANGE THE CODE BELOW
const myNumbers = [1, 2, 3, 4, 5];
doubleNumbers(myNumbers);

// not important
export {};
